{
  "display_name": "Trade Worker",
  "description": "Execution limits and exchange enable flags (read by trade-worker)",
  "sections": {
    "trade": {
      "title": "Execution Limits",
      "description": "trade:* keys used on the order path. Kill switch is shared with Agent risk UI.",
      "icon": "zap",
      "priority": 10,
      "fields": {
        "kill_switch": false,
        "default_leverage": 10,
        "max_position_size": 1000,
      },
      "descriptions": {
        "kill_switch": "trade:kill_switch — reject new orders when truthy (also OR'd with global:kill_switch).",
        "default_leverage": "Default futures leverage when the signal omits leverage.",
        "max_position_size": "Max notional position size (USD) per trade.",
      },
      "options": {
        "default_leverage": [1, 5, 10, 20, 50, 100],
        "max_position_size": [100, 500, 1000, 5000, 10000],
      },
    },
    "exchanges": {
      "title": "Exchanges",
      "description": "exchange:<venue>:enabled — router skips disabled venues",
      "icon": "layers",
      "priority": 20,
      "fields": {
        "binance_enabled": false,
        "mexc_enabled": true,
        "bybit_enabled": false,
      },
      "descriptions": {
        "binance_enabled": "Allow Binance as a routing target.",
        "mexc_enabled": "Allow MEXC as a routing target.",
        "bybit_enabled": "Allow Bybit as a routing target.",
      },
    },
  },
}
